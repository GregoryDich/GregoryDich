<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the optional Standalone app only.

    com.apple.security.device.audio-input  the app opens the input device itself; without it
                                           the hardened runtime denies microphone access and
                                           the audio device selector shows no inputs.

  Nothing else: no JIT, no unsigned memory, no library-validation exception (see
  entitlements.plist for the reasoning). NSMicrophoneUsageDescription lives in Info.plist.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
