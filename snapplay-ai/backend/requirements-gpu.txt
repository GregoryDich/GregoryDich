# GPU worker extras for SNAPPLAY_PIPELINE=local and the worker images. Install on top of
# requirements.txt, then add basic-pitch WITHOUT its dependency list (its default backend
# on Linux is TensorFlow; the ONNX checkpoint runs on onnxruntime-gpu instead):
#   pip install -r requirements.txt -r requirements-gpu.txt
#   pip install --no-deps basic-pitch==0.4.0
# Pinned coherently for the nvidia/cuda:12.4.1-cudnn-runtime base (infra/Dockerfile.worker):
# the +cu124 torch builds ship the CUDA 12.4 / cuDNN 9 user-space libraries that
# onnxruntime-gpu 1.20 (CUDA 12.x, cuDNN 9) also expects.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchaudio==2.6.0+cu124
demucs==4.0.1
onnxruntime-gpu==1.20.2
# basic-pitch 0.4.0 runtime dependencies (its metadata: librosa, mir_eval, numpy,
# pretty_midi, resampy<0.4.3, scikit-learn, scipy, typing_extensions).
librosa==0.11.0
mir_eval==0.8.2
pretty_midi==0.2.10
resampy==0.4.2
scikit-learn>=1.3
scipy>=1.11
typing_extensions>=4.5
